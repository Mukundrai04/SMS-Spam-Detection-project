# SMS-Spam-Detection-project
MS Spam Detection is a machine learning system that identifies whether a message is spam or not spam (ham). It analyzes the text of SMS messages and learns patterns using training data. The model then predicts if new incoming messages are spam. 📩🤖
